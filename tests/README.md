# firebase-scheduler tests

Integration / Staging tests for the scheduler.
We rely on python & pytests for this part.

Required:

env:
- `URL_FIREBASE_FUNCTIONS`: points to your root firebase functions url.